{% extends "admin/base_admin.html.twig" %}

{% block body %}

    <div class="container py-5">
    <a href="{{ path('admin_user_list') }}" class="btn btn-secondary mt-3">Revenir</a>
    <h1>Modifier une histoire</h1>

        {{ include('admin/userList/_form.html.twig', {'button_label': 'Modifier'}) }}

        {{ include('admin/userList/_delete_form.html.twig') }}

    </div>


{% endblock %}
