{% extends "admin/base_admin.html.twig" %}

{% block body %}
    <div class="container py-5">
            <a href="{{ path('product_index') }}" class="btn btn-secondary mt-3">Revenir</a>

            <h1>Ajouter un produit</h1>

            {{ include('admin/product/_form.html.twig') }}
    </div>

{% endblock %}
