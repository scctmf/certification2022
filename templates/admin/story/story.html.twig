{% extends "admin/base_admin.html.twig" %}

{% block body %}
<div class="col-12 mt-3">
            <div class="card">
                <div class="card-header">
                    <h3>Formulaire d'ajout d'Histoires</h3>
                </div>
                <div class="card">
                   
                        {{ form_start(form, {'attr' : {'class': 'mx-5'} }) }}

                        </div>
                        <div>

                        {{ form_widget(form, { 'attr': {'class': 'mx-5 px-5' } }) }}
                        
                            
                        
                        <button class="btn btn-primary mt-3" type="submit"><i class="fas fa-paper-plane"></i> Envoyer</button>

                        {{ form_end(form) }}

                  
                </div>
            </div>
        </div>
{% endblock %}
